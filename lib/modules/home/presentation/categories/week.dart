import 'package:flutter/material.dart';

class WeekCategory extends StatelessWidget {
  const WeekCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("WeekCategory"),
    );
  }
}