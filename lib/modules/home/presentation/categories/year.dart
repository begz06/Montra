import 'package:flutter/material.dart';

class YearCategory extends StatelessWidget {
  const YearCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("YearCategory"),
    );
  }
}