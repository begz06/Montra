import 'package:flutter/material.dart';

class MonthCategory extends StatelessWidget {
  const MonthCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MonthCategory"),
    );
  }
}